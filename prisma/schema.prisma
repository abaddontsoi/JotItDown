generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "mongodb"
    url      = env("DATABASE_URL")
}

model note {
    id String @id @default(auto()) @map("_id") @db.ObjectId
}

model category {
    id String @id @default(auto()) @map("_id") @db.ObjectId
}